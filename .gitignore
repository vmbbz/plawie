# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
/test-results/
/.netlify/
/site/assets/vrm/
/site/assets/js/product-analytics-config.js

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/build/
build_trash/
.tmp/
*.apk
*.aab

# Dependency-pack private signing keys must live in a release secret manager
# or an external encrypted key directory, never anywhere in this repository.
**/signing-private.pem

# MLC-LLM Models (Huge binaries, usually excluded)
# assets/mlc_models/
# mlc_models/

# Native Node runtime candidates are large and provenance-sensitive.
# Package locally for diagnostics, but do not commit an unreviewed binary.
android/app/src/main/jniLibs/arm64-v8a/libplawie_node.so
android/app/src/main/jniLibs/arm64-v8a/libplawie_node.so.manifest.json
android/app/src/main/jniLibs/arm64-v8a/libnode.so
android/app/src/main/jniLibs/arm64-v8a/libnode.so.manifest.json
android/app/src/main/jniLibs/arm64-v8a/libplawie_node_bridge.so
android/app/src/main/jniLibs/arm64-v8a/libplawie_node_bridge.so.manifest.json

# Local native Node source/build products stay outside version control.
build/native-node/

# Isolated Superpowers/Codex implementation worktrees.
.worktrees/

# Local Netlify folder
.netlify
